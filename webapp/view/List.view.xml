<mvc:View
    controllerName="acristache.com.sap.training.ux402.listdetail.ux402listdetail.controller.List"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:f="sap.f"
    xmlns="sap.m"
>
    <f:DynamicPage
        id="dynamicPageOverviewId"
        headerExpanded="true"
        toggleHeaderOnTitleClick="true"
    >
        <f:content>
            <List
                id="list"
                items="{/UX_C_Carrier_TP}"
                busyIndicatorDelay="0"
                growing="true"
                growingThreshold="10"
                growingScrollToLoad="true"
                mode="{= ${device>/system/phone} ? 'None' : 'SingleSelectMaster'}"
                selectionChange="onSelect"
            >
                <items>
                    <ObjectListItem
                        id="objectListItem"
                        title="{Carrname}"
                        intro="{Carrid}"
                        type="{= ${device>/system/phone} ? 'Active' : 'Inactive'}"
                        press="onSelect"
                    />
                </items>
            </List>
        </f:content>
    </f:DynamicPage>
</mvc:View>
